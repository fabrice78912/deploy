apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: frontend
  ports:
    - name: http
      protocol: TCP
      port: 80        # Exposé à l’extérieur (LoadBalancer)
      targetPort: 80 # Port réel dans ton container
#    - name: https
#      protocol: TCP
#      port: 443       # Exposé à l’extérieur (LoadBalancer)
#      targetPort: 443 # Port réel dans ton container