#replicaCount: 4
namespace: staging

env:
  API_HOST: backend-service.dev.svc.cluster.local
  API_PORT: "80"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

#service:
#  type: Loadbalancer
